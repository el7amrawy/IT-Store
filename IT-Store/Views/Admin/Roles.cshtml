@model IEnumerable<IdentityRole<int>>
@{
	ViewData["Title"] = "Roles";
	Layout = "~/Views/Shared/_Admin-Layout.cshtml";
}

<a class=" m-3 btn bg-primary text-white" asp-action="AddRole" asp-asp-controller="Admin">Add Role</a>

<div class="row">
	<div class="col-12 col-lg-8 col-xxl-12 d-flex">
		<div class="card flex-fill">
			<div class="card-header">

				<h5 class="card-title mb-0">Roles</h5>
			</div>
			<table class="table table-hover my-0">
				<thead>
					<tr>
						<th class="d-none d-xl-table-cell">@Html.DisplayNameFor(model => model.Id)</th>
						<th>@Html.DisplayNameFor(model => model.Name)</th>
						<th class="d-none d-md-table-cell"></th>
					</tr>
				</thead>
				<tbody>
					@foreach (var item in Model)
					{
						<tr>
							<td class="d-none d-xl-table-cell">@Html.DisplayFor(modelItem => item.Id)</td>
							<td>@Html.DisplayFor(modelItem => item.Name)</td>
							<td class="d-none d-xl-table-cell">
								<a class="btn text-white bg-danger" asp-action="DeleteRole" asp-controller="Admin" asp-route-id="@item.Id">
									<i class="align-middle" data-feather="delete"></i> <span class="align-middle">Delete</span>
								</a>
							</td>
						</tr>
					}
				</tbody>
			</table>
		</div>
	</div>
</div>